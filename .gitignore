__pycache__/
*.pyc
.DS_Store

# Tool caches. These write their own `.gitignore: *`, so leaving them out worked by
# accident; one of them had landed inside site/public/, the directory published verbatim
# to Pages.
.pytest_cache/
.ruff_cache/
