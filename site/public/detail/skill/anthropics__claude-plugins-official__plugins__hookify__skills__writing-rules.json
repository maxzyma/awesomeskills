{"grounding": {"function": {"en": {"io": "Accepts user requirements for rule triggering conditions and outputs formatted Markdown rule files with YAML frontmatter.", "boundary": "It provides rule writing guidance and generates rule file definitions, but does not execute the hooks or enforce runtime security directly.", "dependencies": ["none"], "scenarios": ["Creating bash hook rules to detect or warn against dangerous shell commands like rm -rf or sudo", "Writing file edit hook rules to prevent committing debugging logs or sensitive environment variables", "Configuring advanced multi-condition rules using specific frontmatter operators and field filters"]}, "zh": {"io": "接收用户对触发逻辑与提示信息的需求描述，输出带有 YAML 元数据的标准格式 Markdown 规则文件。", "boundary": "仅用于指导规则编写和生成规则文件定义，不负责实际的运行时拦截执行或非 Hookify 配置管理。", "dependencies": ["none"], "scenarios": ["创建 Bash 钩子规则以警告或拦截如 rm -rf 或 sudo 等高危 Shell 命令", "编写文件编辑钩子规则以检测 console.log 或敏感密钥泄露", "配置基于字段过滤和正则匹配的多条件高级 Hookify 规则"]}}}, "frontmatter": {"issues": [], "headings": 23, "code_blocks": 15}}
