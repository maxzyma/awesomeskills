{"grounding": {"function": {"en": {"io": "Input is ./my-agent/build-sheet.json plus runtime launch configuration and an ANTHROPIC_API_KEY held in the user's shell. Outputs are four JSON payloads, launch.sh, checkpoint ID files after user execution, validation findings, and a phase-state update.", "boundary": "The described generators and validator make no network calls; launch.sh is executed by the user and performs external creation. The key must not enter chat, payloads, files, or logs. The attached evidence does not include the scripts or API specification, and successful staging does not establish a successful deployment.", "dependencies": ["Python 3", "scripts/payload_generator.py", "scripts/launch_script_writer.py", "scripts/payload_validator.py", "curl in the user execution environment", "User-owned ANTHROPIC_API_KEY supplied only through the shell environment", "Claude Managed Agent API access"], "scenarios": ["Generate environment, agent, session, and kickoff payload files.", "Create a launcher that chains returned IDs and resumes from saved checkpoints.", "Validate payload structure and scan for Anthropic API-key leakage.", "Prepare a user-operated launch sequence and observe its first poll.", "Advance launcher state to the grade-and-iterate phase after launch."]}, "zh": {"io": "输入为 ./my-agent/build-sheet.json、运行时启动配置，以及仅保存在用户 shell 中的 ANTHROPIC_API_KEY。输出为四个 JSON 载荷、launch.sh、用户执行后生成的检查点 ID 文件、验证发现和阶段状态更新。", "boundary": "所述生成器和验证器不发起网络请求；launch.sh 由用户执行并创建外部资源。密钥不得进入对话、载荷、文件或日志。附加证据不包含脚本或 API 规范，暂存成功也不能证明部署成功。", "dependencies": ["Python 3", "scripts/payload_generator.py", "scripts/launch_script_writer.py", "scripts/payload_validator.py", "用户执行环境中的 curl", "仅通过 shell 环境提供的用户自有 ANTHROPIC_API_KEY", "Claude Managed Agent API 访问权限"], "scenarios": ["生成环境、代理、会话和启动任务载荷文件。", "创建可串联返回 ID 并根据已保存检查点续跑的启动脚本。", "验证载荷结构并扫描 Anthropic API 密钥泄漏。", "准备由用户执行的启动序列并观察首次轮询。", "启动后将启动器状态推进到评分迭代阶段。"]}}}, "frontmatter": {"issues": [], "headings": 6, "code_blocks": 5}}
